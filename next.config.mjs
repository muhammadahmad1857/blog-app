/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['i.pinimg.com']
    },
    video:{
        domains: ['videos.pexels.com']
    }
};

export default nextConfig;
